# Plano de Implementação: Pokédex 3D Interativa

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformar a Pokédex da página `pages/pokedex.html` em uma Pokédex 3D interativa em Three.js, oferecendo rotação/inclinação via mouse, animação de aproximação (zoom ao clicar), suporte a overlay em PNG e busca em tempo real via PokéAPI.

**Architecture:** Cena 3D em Three.js com modelo procedural de fallback e suporte ao `GLTFLoader` para arquivos `.glb`/`.gltf`, sincronizada com transição de câmera via `lerp`, overlay HTML/CSS customizado com as variáveis do `global.css` e lógica de consumo da PokéAPI em JavaScript ES6+.

**Tech Stack:** Three.js, HTML5, CSS3 (Grid, Flexbox, Variáveis CSS), JavaScript ES6+ (Fetch API, Async/Await).

## Global Constraints

- **Paleta de Cores**: `--bg: #0c0e12`, `--bg-elevated: #141820`, `--bg-card: #1a1f2a`, `--red: #d32f2f`, `--red-dark: #b71c1c`, `--yellow: #ffd54f`, `--text: #f5f5f5`, `--text-muted: #9aa3b2`.
- **Tipografia**: `"Outfit"`, `"Space Grotesk"`, `"Space Mono"`, `"Baloo 2"`.
- **Caminho da Spec**: `docs/superpowers/specs/2026-08-10-pokedex-3d-design.md`.

---

### Task 1: Reestruturação HTML e Estilização CSS para a Pokédex 3D e Overlay

**Files:**
- Modify: `pages/pokedex.html`
- Modify: `assets/css/pages/pokedex.css`

**Interfaces:**
- Produces: Elementos DOM `#pokedex3d-container`, `#pokedex3d-canvas`, `#pokedex3d-overlay`, `#pokeInput`, `#searchBtn`, `#resetViewBtn`, `#lens` e `#screen`.

- [ ] **Step 1: Atualizar o HTML em `pages/pokedex.html`**

Adicionar as bibliotecas Three.js e GLTFLoader via CDN/scripts no `<head>` ou antes do fecha `</body>`. Substituir a antiga div `.device` pela estrutura 3D e overlay:

```html
<main class="pokedex-page">
  <div id="pokedex3d-container" class="pokedex3d-container">
    <canvas id="pokedex3d-canvas"></canvas>
    
    <div id="pokedex3d-overlay" class="pokedex3d-overlay">
      <div class="overlay-frame">
        <div class="overlay-header">
          <div class="lens state-idle" id="lens"></div>
          <div class="dots"><span></span><span></span></div>
          <div class="brand">POKÉDEX 3D</div>
          <button id="resetViewBtn" class="btn-reset-3d" title="Voltar visão 3D">↺ Visão 3D</button>
        </div>

        <div class="screen-frame">
          <div class="screen" id="screen">
            <div class="screen-label">Nº scanner</div>
            <div class="placeholder" id="placeholder">
              <span class="big">◌</span>
              Digite o nome de um pokémon<br>e pressione buscar
            </div>
          </div>
        </div>

        <div class="controls">
          <input type="text" id="pokeInput" placeholder="ex: pikachu" autocomplete="off">
          <button id="searchBtn" aria-label="Buscar">
            <svg viewBox="0 0 24 24" fill="none" stroke="#4a2f00" stroke-width="2.5" stroke-linecap="round">
              <circle cx="11" cy="11" r="7"/>
              <line x1="21" y1="21" x2="16.65" y2="16.65"/>
            </svg>
          </button>
        </div>
      </div>
    </div>

    <div class="hint-3d" id="hint3d">
      <span>✦ Passe o mouse para inclinar • Clique para aproximar e buscar</span>
    </div>
  </div>
</main>
<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/three@0.128.0/examples/js/loaders/GLTFLoader.js"></script>
<script src="../assets/js/pokedex3d.js"></script>
<script src="../assets/js/pokedex.js"></script>
```

- [ ] **Step 2: Estilizar a Pokédex 3D em `assets/css/pages/pokedex.css`**

Atualizar o CSS utilizando as variáveis do `global.css` (`var(--bg-card)`, `var(--red)`, `var(--yellow)`, `var(--text)`), configurando a transição da overlay em PNG e os botões:

```css
.pokedex3d-container {
  position: relative;
  width: 100%;
  max-width: 800px;
  height: 600px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

#pokedex3d-canvas {
  width: 100%;
  height: 100%;
  display: block;
  cursor: pointer;
}

.pokedex3d-overlay {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0.95);
  width: 100%;
  max-width: 440px;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.35s ease, transform 0.35s ease;
  z-index: 10;
}

.pokedex3d-container.zoomed .pokedex3d-overlay {
  opacity: 1;
  pointer-events: auto;
  transform: translate(-50%, -50%) scale(1);
}

.btn-reset-3d {
  background: rgba(255, 255, 255, 0.12);
  border: 1px solid rgba(255, 255, 255, 0.25);
  color: var(--white);
  padding: 4px 10px;
  border-radius: 999px;
  font-size: 11px;
  font-weight: 600;
  cursor: pointer;
  margin-left: auto;
  transition: background 0.2s ease, border-color 0.2s ease;
}

.btn-reset-3d:hover {
  background: var(--red);
  border-color: var(--red-dark);
}

.hint-3d {
  position: absolute;
  bottom: 12px;
  left: 50%;
  transform: translateX(-50%);
  font-size: 11px;
  color: var(--text-muted);
  letter-spacing: 1px;
  pointer-events: none;
  transition: opacity 0.3s ease;
}

.pokedex3d-container.zoomed .hint-3d {
  opacity: 0;
}
```

- [ ] **Step 3: Commit das alterações da Task 1**

```bash
git add pages/pokedex.html assets/css/pages/pokedex.css
git commit -m "feat(pokedex): add 3D viewport HTML layout and CSS overlay styling"
```

---

### Task 2: Inicialização da Cena 3D Three.js, Fallback Procedural e Inclinação ao Mover o Mouse

**Files:**
- Create: `assets/js/pokedex3d.js`

**Interfaces:**
- Produces: `window.Pokedex3D` objeto global exposto com estado `isZoomed`, métodos `zoomIn()`, `resetView()`, e callbacks de interação.

- [ ] **Step 1: Criar o módulo `assets/js/pokedex3d.js`**

Implementar o código de inicialização do Three.js (Scene, Camera, WebGLRenderer, AmbientLight, DirectionalLight), o fallback de malha 3D e o ouvinte de movimento do mouse:

```javascript
(function () {
  const container = document.getElementById("pokedex3d-container");
  const canvas = document.getElementById("pokedex3d-canvas");

  if (!container || !canvas) return;

  let scene, camera, renderer, pokedexGroup;
  let isZoomed = false;
  let targetRotationX = 0;
  let targetRotationY = 0;

  const initialCamPos = { x: 0, y: 0, z: 5.5 };
  const zoomedCamPos = { x: 0, y: 0, z: 2.8 };

  function init() {
    scene = new THREE.Scene();

    camera = new THREE.PerspectiveCamera(
      45,
      container.clientWidth / container.clientHeight,
      0.1,
      1000
    );
    camera.position.set(initialCamPos.x, initialCamPos.y, initialCamPos.z);

    renderer = new THREE.WebGLRenderer({ canvas: canvas, alpha: true, antialias: true });
    renderer.setSize(container.clientWidth, container.clientHeight);
    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));

    const ambientLight = new THREE.AmbientLight(0xffffff, 0.75);
    scene.add(ambientLight);

    const dirLight = new THREE.DirectionalLight(0xffffff, 1.0);
    dirLight.position.set(5, 8, 5);
    scene.add(dirLight);

    pokedexGroup = new THREE.Group();
    scene.add(pokedexGroup);

    createFallbackModel();
    loadGLTFModel();

    addEventListeners();
    animate();
  }

  function createFallbackModel() {
    const bodyGeo = new THREE.BoxGeometry(2.2, 3.2, 0.4);
    const bodyMat = new THREE.MeshStandardMaterial({ color: 0xd32f2f, roughness: 0.3, metalness: 0.2 });
    const body = new THREE.Mesh(bodyGeo, bodyMat);
    pokedexGroup.add(body);

    const screenFrameGeo = new THREE.BoxGeometry(1.6, 1.6, 0.05);
    const screenFrameMat = new THREE.MeshStandardMaterial({ color: 0x141820 });
    const screenFrame = new THREE.Mesh(screenFrameGeo, screenFrameMat);
    screenFrame.position.set(0, 0.3, 0.2);
    pokedexGroup.add(screenFrame);

    const lensGeo = new THREE.SphereGeometry(0.18, 16, 16);
    const lensMat = new THREE.MeshStandardMaterial({ color: 0x4aa8e0, roughness: 0.1, metalness: 0.8 });
    const lens = new THREE.Mesh(lensGeo, lensMat);
    lens.position.set(-0.7, 1.3, 0.22);
    pokedexGroup.add(lens);
  }

  function loadGLTFModel() {
    if (typeof THREE.GLTFLoader === "undefined") return;
    const loader = new THREE.GLTFLoader();
    loader.load(
      "../assets/models/pokedex.glb",
      (gltf) => {
        pokedexGroup.clear();
        pokedexGroup.add(gltf.scene);
      },
      undefined,
      (err) => {
        console.log("Modelo .glb não encontrado. Usando Pokédex 3D de fallback.");
      }
    );
  }

  function addEventListeners() {
    container.addEventListener("pointermove", (e) => {
      if (isZoomed) return;
      const rect = container.getBoundingClientRect();
      const x = ((e.clientX - rect.left) / container.clientWidth) * 2 - 1;
      const y = -(((e.clientY - rect.top) / container.clientHeight) * 2 - 1);
      targetRotationY = x * 0.35;
      targetRotationX = -y * 0.25;
    });

    window.addEventListener("resize", () => {
      camera.aspect = container.clientWidth / container.clientHeight;
      camera.updateProjectionMatrix();
      renderer.setSize(container.clientWidth, container.clientHeight);
    });
  }

  function animate() {
    requestAnimationFrame(animate);

    if (!isZoomed) {
      pokedexGroup.rotation.y += (targetRotationY - pokedexGroup.rotation.y) * 0.08;
      pokedexGroup.rotation.x += (targetRotationX - pokedexGroup.rotation.x) * 0.08;

      camera.position.x += (initialCamPos.x - camera.position.x) * 0.08;
      camera.position.y += (initialCamPos.y - camera.position.y) * 0.08;
      camera.position.z += (initialCamPos.z - camera.position.z) * 0.08;
    } else {
      pokedexGroup.rotation.y += (0 - pokedexGroup.rotation.y) * 0.08;
      pokedexGroup.rotation.x += (0 - pokedexGroup.rotation.x) * 0.08;

      camera.position.x += (zoomedCamPos.x - camera.position.x) * 0.08;
      camera.position.y += (zoomedCamPos.y - camera.position.y) * 0.08;
      camera.position.z += (zoomedCamPos.z - camera.position.z) * 0.08;
    }

    renderer.render(scene, camera);
  }

  window.Pokedex3D = {
    zoomIn: function () {
      isZoomed = true;
      container.classList.add("zoomed");
    },
    resetView: function () {
      isZoomed = false;
      container.classList.remove("zoomed");
    },
    isZoomed: function () {
      return isZoomed;
    }
  };

  init();
})();
```

- [ ] **Step 2: Commit do módulo 3D em `assets/js/pokedex3d.js`**

```bash
git add assets/js/pokedex3d.js
git commit -m "feat(pokedex): add Three.js scene, procedural fallback, tilt parallax and Pokedex3D module API"
```

---

### Task 3: Conexão de Eventos de Clique para Zoom e Restauração de Visão 3D

**Files:**
- Modify: `assets/js/pokedex3d.js`
- Modify: `pages/pokedex.html`

**Interfaces:**
- Consumes: `window.Pokedex3D.zoomIn()`, `window.Pokedex3D.resetView()`
- Produces: Clique no canvas dispara zoom; clique em `#resetViewBtn` desfaz o zoom.

- [ ] **Step 1: Configurar os eventos de clique no canvas e no botão de reset**

Em `assets/js/pokedex3d.js`, registrar o clique no canvas para ativar o zoom e registrar a ação no botão `#resetViewBtn`:

```javascript
canvas.addEventListener("click", () => {
  if (!window.Pokedex3D.isZoomed()) {
    window.Pokedex3D.zoomIn();
  }
});

const resetBtn = document.getElementById("resetViewBtn");
if (resetBtn) {
  resetBtn.addEventListener("click", (e) => {
    e.stopPropagation();
    window.Pokedex3D.resetView();
  });
}
```

- [ ] **Step 2: Commit da Task 3**

```bash
git add assets/js/pokedex3d.js
git commit -m "feat(pokedex): connect canvas click zoom and reset view interactions"
```

---

### Task 4: Atualização da Lógica da PokéAPI em `assets/js/pokedex.js`

**Files:**
- Modify: `assets/js/pokedex.js`

**Interfaces:**
- Consumes: `#pokeInput`, `#searchBtn`, `#screen`, `#lens`
- Produces: Atualização dos dados do Pokémon e dos estados da lente (`idle`, `loading`, `found`, `error`) dentro do container overlay.

- [ ] **Step 1: Revisar e adaptar `assets/js/pokedex.js`**

Garantir que a busca via PokéAPI atualize corretamente a tela e a lente de status dentro da nova estrutura overlay, acionando automaticamente o zoom se o usuário realizar a busca:

```javascript
const campoPokemon = document.getElementById("pokeInput");
const botaoBuscar = document.getElementById("searchBtn");
const tela = document.getElementById("screen");
const lente = document.getElementById("lens");

function alterarLente(estado) {
  if (lente) {
    lente.className = "lens state-" + estado;
  }
}

function mostrarTelaInicial() {
  tela.innerHTML = `
    <div class="screen-label">Nº scanner</div>
    <div class="placeholder">
      <span class="big">◌</span>
      Digite o nome de um pokémon<br>e pressione buscar
    </div>
  `;
}

function mostrarCarregando() {
  tela.innerHTML = `
    <div class="screen-label">Nº scanner</div>
    <div class="placeholder">
      <span class="big">◐</span>
      Buscando...
    </div>
  `;
}

function mostrarErro(nomePokemon) {
  tela.innerHTML = `
    <div class="screen-label">Nº scanner</div>
    <div class="error-msg">
      <span class="big">✕</span>
      Pokémon "${nomePokemon}" não encontrado.<br>
      Confira o nome e tente novamente.
    </div>
  `;
}

const TIPOS_PT = {
  normal: "normal", fire: "fogo", water: "água", electric: "elétrico",
  grass: "planta", ice: "gelo", fighting: "lutador", poison: "veneno",
  ground: "terra", flying: "voador", psychic: "psíquico", bug: "inseto",
  rock: "pedra", ghost: "fantasma", dragon: "dragão", dark: "sombrio",
  steel: "aço", fairy: "fada"
};

const HABILIDADES_PT = {
  overgrow: "Crescimento Excessivo", blaze: "Chama", torrent: "Torrente",
  static: "Estático", pressure: "Pressão", synchronize: "Sincronismo",
  intimidate: "Intimidação", levitate: "Levitação", sturdy: "Robustez",
  adaptability: "Adaptabilidade"
};

function mostrarPokemon(pokemon) {
  const numero = String(pokemon.id).padStart(3, "0");
  const imagemPokemon =
    pokemon.sprites.other?.showdown?.front_default ||
    pokemon.sprites.other?.official_artwork?.front_default ||
    pokemon.sprites.front_default;

  const tipos = pokemon.types
    .map(tipo => `<span class="type-pill">${TIPOS_PT[tipo.type.name] || tipo.type.name}</span>`)
    .join("");

  const altura = (pokemon.height / 10).toFixed(1);
  const peso = (pokemon.weight / 10).toFixed(1);
  const habilidade = HABILIDADES_PT[pokemon.abilities[0]?.ability?.name] || pokemon.abilities[0]?.ability?.name || "Desconhecida";

  tela.innerHTML = `
    <div class="screen-label">Nº scanner</div>
    <div class="sprite-wrap">
      <img src="${imagemPokemon}" alt="${pokemon.name}">
    </div>
    <div class="poke-number">Nº ${numero}</div>
    <div class="poke-name">${pokemon.name}</div>
    <div class="poke-types">${tipos}</div>
    <div class="poke-info">
      <div><strong>Altura</strong><br>${altura} m</div>
      <div><strong>Peso</strong><br>${peso} kg</div>
      <div><strong>Habilidade</strong><br>${habilidade}</div>
    </div>
  `;
}

async function buscarPokemon() {
  const pesquisa = campoPokemon.value.trim().toLowerCase();

  if (window.Pokedex3D && !window.Pokedex3D.isZoomed()) {
    window.Pokedex3D.zoomIn();
  }

  if (!pesquisa) {
    mostrarTelaInicial();
    alterarLente("idle");
    return;
  }

  alterarLente("loading");
  mostrarCarregando();

  try {
    const resposta = await fetch(`https://pokeapi.co/api/v2/pokemon/${encodeURIComponent(pesquisa)}`);
    if (!resposta.ok) throw new Error("Pokémon não encontrado");

    const pokemon = await resposta.json();
    mostrarPokemon(pokemon);
    alterarLente("found");
  } catch (erro) {
    mostrarErro(pesquisa);
    alterarLente("error");
  }
}

if (botaoBuscar) botaoBuscar.addEventListener("click", buscarPokemon);
if (campoPokemon) {
  campoPokemon.addEventListener("keydown", (e) => {
    if (e.key === "Enter") buscarPokemon();
  });
}
```

- [ ] **Step 2: Commit da Task 4**

```bash
git add assets/js/pokedex.js
git commit -m "feat(pokedex): adapt PokéAPI logic and lens indicator for 3D overlay integration"
```

---

### Task 5: Validação Final e Testes da Aplicação

- [ ] **Step 1: Testar no navegador**
  - Abrir `pages/pokedex.html` no navegador.
  - Verificar a renderização do modelo 3D em Three.js.
  - Testar o movimento do mouse para confirmar o efeito de inclinação parallax.
  - Clicar na Pokédex 3D para testar a transição de zoom da câmera e abertura da overlay.
  - Pesquisar um Pokémon (`pikachu`, `gengar`) para testar requisições, sprites animados, tipos traduzidos e alteração da cor da lente (`state-loading` -> `state-found`).
  - Testar a busca de um Pokémon inválido para validar o estado `state-error`.
  - Clicar no botão **"↺ Visão 3D"** para validar o retorno da visão tridimensional.
